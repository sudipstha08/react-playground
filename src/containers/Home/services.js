import axios, { AxiosResponse } from 'axios';
import API from '../../urls';


class HomeServices {
    GetHomeContent() {
        aisos.post(API.CreateNewProject)
        return true;    
    }
}

export default new HomeServices();
